<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/background_main_gradient"
    android:padding="24dp">

    <!-- Header -->
    <TextView
        android:id="@+id/titleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Choose Your Hero Power"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="@color/text_primary_light"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="24dp" />

    <!-- Description -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Each hero power has unique abilities that can turn the tide of battle. Choose wisely!"
        android:textSize="16sp"
        android:textColor="@color/text_secondary_light"
        android:gravity="center"
        android:layout_marginBottom="32dp" />

    <!-- Hero Powers List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/heroPowerRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="8dp" />

    <!-- Confirm Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/confirmButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Confirm Selection"
        android:textSize="18sp"
        android:textStyle="bold"
        android:enabled="false"
        app:backgroundTint="@color/secondary_gold"
        app:cornerRadius="12dp"
        android:layout_marginTop="16dp" />

</LinearLayout>
